# CareerConsole 真实用户体验审查报告

> 审查日期：2026-07-27  
> 审查对象：当前改造后的 CareerConsole Web 产品  
> 审查目标：以普通求职用户视角发现阻断、误导、理解成本、操作负担和产品能力缺口  
> 审查边界：本轮只分析并形成整改依据，不修改核心业务逻辑或前端代码  
> 关联文档：`CAREER_CONSOLE_UX_CONFIGURATION_DEPLOYMENT_REVIEW.md`

## 1. 审查结论

上一轮改造已经解决了一批基础问题：

- 主导航已按“今日、机会、申请、我的资料、消息与确认、设置”组织；
- 移动端已有抽屉导航实现；
- 档案和导入页面降低了技术信息密度；
- 设置已分类并加入渐进披露；
- 未知路由已有真正的 404 页面；
- 常见文件导入入口已经中文化；
- 当前浏览器控制台未发现前端 `error` 或 `warn`。

但产品目前仍更接近：

```text
一套成熟的求职领域模型
+ 多个可以独立工作的业务页面
+ 一套面向工程人员的配置控制台
```

还没有完全成为：

```text
围绕用户目标组织
→ 自动判断当前状态
→ 推荐唯一或少量明确下一步
→ 解释为什么受阻
→ 隐藏内部对象和执行机制
→ 连续完成求职任务
```

当前最严重的问题不是视觉样式，而是“系统掌握了很多数据，却没有稳定地替用户作出正确的界面判断”。例如：

- 用户已有确认档案、目标岗位、Final 材料和申请记录，首页仍建议“完善我的经历”；
- 岗位要求为 0、证据为 0、匹配分为 0，页面仍突出“生成材料”和“创建申请”；
- Agent 分析与确定性分析作为两套并列流程直接交给用户选择；
- 邮箱配置与招聘邮件日常处理混在同一个页面；
- 大量禁用按钮没有说明禁用原因；
- 材料、申请和诊断页面仍暴露哈希、版本、Schema、内部状态值；
- “所有配置都有测试按钮”被机械实现，导致外观、日期格式等设置也出现无法解释价值的测试操作。

总体判断：

> CareerConsole 的领域能力已经具备产品化基础，但交互层仍以“展示系统对象”为主，而不是以“帮助用户完成决定”为主。下一阶段应优先重构状态判断、连续任务流和用户语言，不应继续横向增加页面或配置项。

## 2. 审查方法与证据

### 2.1 真实浏览器走查

本次通过已连接的 Chrome 访问真实服务：

```text
http://127.0.0.1:8765
```

实际检查了：

- 今日行动台；
- 每日招聘；
- 目标岗位列表与详情；
- 材料列表与详情；
- 申请列表、申请总览与申请详情；
- 任务与日程；
- 面试中心；
- 招聘邮件；
- 全部待确认；
- 职业档案；
- 资料导入；
- 简历内容确认；
- 设置全部七个分类；
- 招聘信息来源；
- Agent 运行记录；
- 服务状态；
- 后台任务。

检查方式包括：

- 页面真实导航；
- 可见 DOM、按钮状态和空状态；
- 首页与申请详情截图；
- 前端控制台错误和警告检查；
- 响应式 CSS 与页面结构复核。

### 2.2 当前测试数据

当前隔离工作区中已有：

- 3 条已确认职业事实；
- 1 个目标岗位；
- 1 份 Final 材料；
- 1 个申请流程；
- 5 条申请状态事件；
- 1 份已导入文档；
- 0 条待确认事实；
- 0 条当前任务、面试和招聘机会。

因此本次不只是空系统审查，也覆盖了“已有部分业务数据，但今天没有新事项”的日常场景。

### 2.3 响应式审查限制

桌面端完成了真实视觉检查。当前浏览器控制接口未提供安全的设备视口切换能力，因此移动端结论来自：

- 已实现的 700px/900px 响应式规则；
- 移动端抽屉和设置分类选择器代码；
- 各页面在单列布局下的表单、按钮和内容顺序推演。

后续修改验收仍需补做 390×844、430×932 等真实移动视口的逐页点击测试。

## 3. 产品核心用户旅程

### 3.1 产品设计期望

```text
建立和维护职业档案
→ 获取当天招聘机会
→ 保存或导入具体岗位
→ 判断岗位是否值得申请
→ 选择简历修改方向
→ 生成并确认申请材料
→ 用户自行投递
→ 邮件识别申请进度
→ 用户确认正式状态
→ 生成任务、提醒和面试准备
→ 结果与反馈回流职业档案
```

### 3.2 当前用户实际感受到的路径

```text
进入首页看到多个统计卡
→ 根据固定优先级收到一个建议
→ 进入某个业务对象列表
→ 选择 Agent 或确定性执行方式
→ 查看分数、版本、候选、事件流等对象
→ 自己判断下一页面和下一操作
→ 遇到禁用按钮时自行推测依赖
→ 到设置或另一个业务页补配置
```

差异的本质是：

> 系统内部已经有业务链，前端却仍要求用户理解这条业务链的实现方式。

## 4. 十个最严重的体验问题

### UX-01 首页建议与真实业务状态不一致

**分类：明确缺陷**  
**优先级：P1**

当前用户已有确认事实、岗位、Final 材料和申请，但首页仍建议：

```text
完善我的经历
```

当前算法只关注：

- 待确认数量；
- 今日任务；
- 今日机会；
- 否则默认完善经历。

它没有判断：

- 档案是否已经达到可用基线；
- 是否有正在推进的申请；
- 是否有已生成但尚未使用的材料；
- 是否存在长期未更新的申请；
- 招聘来源是否未配置或同步失败；
- 当前用户最接近完成的业务动作。

这会直接损害用户对首页建议的信任。首页一旦多次给出明显错误建议，用户会把它当作无用装饰。

**目标行为：**

首页下一步应由统一行动决策器产生，至少考虑：

```text
阻断项
→ 有时限的事项
→ 待人工确认
→ 正在进行的申请
→ 可完成的材料/投递动作
→ 数据来源异常
→ 档案缺口
→ 探索新机会
```

### UX-02 岗位数据无效时仍鼓励进入后续正式流程

**分类：明确缺陷 / 数据风险**  
**优先级：P0**

当前岗位详情同时显示：

- 匹配分 0；
- 岗位要求 0 项；
- 证据支持 0；
- Agent 分析 0；
- “硬门槛通过”；
- 可点击“生成材料”；
- 可点击“创建申请”。

“岗位要求为 0”不等于“硬门槛通过”。当前状态容易让用户把“没有可检查条件”理解成“已经满足所有条件”。

如果继续生成材料或创建申请，系统会基于不完整 JD 建立正式业务对象，导致后续材料、匹配和申请决策缺少可靠基础。

**目标行为：**

```text
未提取到岗位要求
→ 标记“尚未完成岗位分析”
→ 主操作改为“检查岗位内容”
→ 提供“重新提取”或“补充 JD”
→ 分析有效后才开放材料和申请主路径
```

如业务允许用户强制继续，应放在高级操作并明确风险，而不是作为默认主按钮。

### UX-03 Agent 与确定性流程直接暴露给普通用户

**分类：体验风险**  
**优先级：P1**

岗位和材料页面同时出现：

- Agent 语义分析；
- 确定性重算；
- Agent 材料候选；
- 本地确定性生成；
- Agent Reviewer；
- 确定性审核。

这些是执行策略，不是用户目标。用户真正想表达的是：

```text
帮我判断是否匹配
帮我规划简历修改方向
帮我生成一份可信材料
```

普通用户没有依据判断应该选择哪套流程，也不应该承担该选择。

**目标行为：**

- 主界面只提供面向目标的动作；
- 系统根据 AI 配置、数据完整性、失败状态自动选择执行策略；
- 若 AI 不可用，显示“将使用本地规则完成基础分析”；
- “重新运行方式、模型、确定性结果、Agent 结果”进入高级详情；
- 对最终结论展示融合结果和依据，而不是两份平行结果。

### UX-04 邮箱配置与招聘邮件日常使用混在一起

**分类：明确缺陷**  
**优先级：P1**

未配置邮箱时，招聘邮件页面直接展示完整 IMAP 配置，并出现：

- 原始状态 `unconfigured`；
- “测试只读连接”；
- “测试邮箱完整配置”；
- 保存；
- 立即同步；
- UID SEARCH、BODY.PEEK、Header 等工程说明。

配置完成后，用户日常进入这里的目标应是：

- 查看新识别的招聘邮件；
- 查看关联申请；
- 确认候选进度；
- 检查同步是否正常。

而不是反复看到账号和协议配置。

**目标行为：**

```text
招聘邮件页
├─ 邮件与识别结果
├─ 待确认进度
├─ 最近同步状态
└─ “管理邮箱连接”入口

设置 → 招聘与邮箱
└─ 账号、凭据、连接测试、同步策略
```

### UX-05 禁用按钮普遍没有解释

**分类：明确缺陷**  
**优先级：P1**

已发现的例子：

- “生成 Agent 材料候选”禁用但没有原因；
- 没有可选岗位时“创建申请流程”仍出现；
- OpenCLI 未配置时“主动获取”禁用；
- BOSS 条件不满足时“立即扫描”禁用；
- QQ 测试通知禁用；
- 邮箱测试和同步按钮禁用。

禁用控件不会告诉用户缺少什么，也无法被键盘焦点稳定访问。用户只能猜测是：

- 页面坏了；
- 网络问题；
- 尚未保存；
- 缺少配置；
- 没有数据；
- 权限不足。

**目标行为：**

每个不可用主操作必须满足至少一种方式：

- 按钮旁明确显示阻塞原因；
- 使用可点击按钮，点击后弹出缺失条件和修复入口；
- 在依赖卡片中列出未满足项；
- 没有任何可执行对象时不显示该按钮。

### UX-06 内部领域对象和审计信息默认暴露过多

**分类：体验风险**  
**优先级：P2**

当前普通业务页面仍出现：

- 事件流 v4；
- 事实版本 2；
- 工作流 v2；
- 哈希值；
- `basic`、`skill`；
- `profile_fact_extraction`；
- `candidate_fact.v1`；
- `succeeded`；
- Schema / Prompt；
- 冻结版本、不可变快照、版本血缘。

这些信息对审计、调试和工业级可追溯性有价值，但默认展示会让产品像数据库管理工具。

**目标行为：**

- 业务页默认展示用户认识的内容、状态、时间和来源；
- “记录详情/审计详情”折叠后再展示版本和哈希；
- 高级诊断页保留完整技术信息；
- 英文枚举必须映射为中文；
- 哈希默认截断且提供复制，不应作为正文。

### UX-07 申请详情存在两套近似进度录入

**分类：明确缺陷**  
**优先级：P1**

同一页面并列：

- “手动添加已确认进度”；
- “添加待确认的进度”。

普通用户很难理解：

- 为什么自己手动输入还需要候选；
- 什么时候应该直接确认；
- 什么时候应该进入待确认；
- 邮件识别结果与这两个表单是什么关系。

当前“候选进度”标题显示 `0 待确认`，列表中却仍展示一条已经确认的候选记录，进一步加重状态矛盾。

**目标行为：**

主入口统一为：

```text
更新申请进度
```

然后根据来源处理：

- 用户主动记录：默认直接确认，提交前显示影响；
- 邮件/Agent/自动识别：进入待确认；
- 用户明确选择“先保存草稿”时才进入候选；
- 已处理候选放入“历史记录”，不继续混在待确认区域。

### UX-08 设置中心仍以系统结构而不是用户任务组织

**分类：体验风险**  
**优先级：P2**

设置虽然已经分类，但仍存在：

- 七个一级分类；
- 默认展示数据库、配置、备份、导出完整绝对路径；
- 招聘与邮箱分类实际只配置 OpenCLI，牛客和邮箱再跳到业务页；
- 外观、常规、安全均出现“测试”按钮；
- 高级页直接暴露租约、审计保留和日志参数；
- 全局“配置已生效”无法准确表达未保存修改。

当前结构让用户感觉是在配置一个服务，而不是设置一个个人求职产品。

**目标行为：**

设置一级分类建议收敛为：

```text
通用
AI
数据来源
通知与自动化
数据与安全
高级
```

牛客、BOSS、邮箱应在“数据来源”中完成配置和测试，业务页面只负责使用。

### UX-09 自动任务运行记录被空跑日志淹没

**分类：明确缺陷**  
**优先级：P2**

自动任务页面当前展示最近 50 条记录，每分钟一条：

```text
schedule · 已完成
提醒 0 · 数据来源 0 · 通知 0
```

这不是对用户有意义的历史，而是调度心跳。它会：

- 掩盖真正执行过的同步和通知；
- 让用户误以为系统每分钟进行了大量工作；
- 增加页面长度和检查成本；
- 让异常更难发现。

**目标行为：**

- 普通记录只展示产生结果、发生跳过或出现异常的运行；
- 连续空跑聚合为“自动任务服务运行正常”；
- 技术心跳放入高级诊断；
- 默认显示最近有效运行、下一次计划和最近失败。

### UX-10 产品缺少统一的“当前是否可继续”判断

**分类：产品能力缺口**  
**优先级：P1**

多个页面只展示局部数据，没有给出决策结论：

- 岗位值不值得继续；
- 为什么匹配分低；
- 还缺什么才能生成材料；
- 当前材料能不能用于投递；
- 当前申请下一步是什么；
- 邮箱为什么不能同步；
- 自动任务是否真的在工作。

用户需要从多个指标和内部状态中自行推理。

建议引入统一的业务状态摘要：

```text
当前结论
阻塞原因
推荐下一步
其他可选操作
查看依据
```

它不是单纯前端组件，而应由应用层提供稳定的业务决策 ViewModel。

## 5. 按用户旅程逐页问题

## 5.1 今日行动台

### 明确缺陷

1. 建议算法与已有业务状态不一致。
2. “检查到期提醒”像需要用户手动触发的系统维护动作，职责错误。
3. 页面存在多个 0 指标和四个空卡片，却没有收敛为空状态。

### 体验风险

1. “找岗位 / 加目标 / 记进度 / 改材料”中的“加目标”含义不清。
2. “开始处理”没有说明将进入什么页面。
3. 首页同时有建议卡、四个快捷入口、四个指标和五个内容区，信息很多但当天没有任何事项。
4. 视觉上首页结构整齐，但空数据时大量等权卡片形成“控制台感”。

### 建议优化

- 无今日事项时切换为简洁的空闲状态；
- 只保留一个主建议和两个次要入口；
- 将系统自动检查改为后台行为，异常时再提示；
- 快捷入口使用明确动词：“查看新机会”“保存目标岗位”“更新申请进度”“准备申请材料”。

## 5.2 每日招聘

### 明确缺陷

1. 空状态没有可点击的配置数据来源 CTA。
2. 导航“今日招聘”与页面“每日招聘”不一致。
3. 无数据时仍显示完整状态筛选控件。

### 产品能力缺口

1. 页面没有显示来源是否可用、最近同步、下一次同步和立即同步。
2. 没有区分“今天确实没有新机会”和“数据来源未运行”。
3. 未向用户解释招聘线索如何变成具体目标岗位。

### 建议优化

空状态应根据原因区分：

```text
未配置来源 → 去配置
来源受阻 → 修复 OpenCLI/登录
尚未同步 → 立即获取今天
已同步且无结果 → 今天暂时没有新机会
筛选无结果 → 清除筛选
```

## 5.3 目标岗位列表

### 明确缺陷

1. 文件按钮仍显示原生英文 `Choose File`。
2. 导航“收藏的岗位”与页面“目标岗位”不一致。
3. `低优先级 · 0` 不解释分数含义、范围和原因。
4. `类型待确认 · v1` 暴露内部版本，且没有待确认处理入口。

### 体验风险

1. 粘贴、上传文件、公开 URL 三种方式平铺，主次不清。
2. URL 安全说明比“如何成功导入岗位”更醒目。
3. 列表卡片缺少申请截止、地点、状态、下一步等决策信息。

### 建议优化

- 默认推荐“粘贴 JD 或上传文件”，URL 作为次要入口；
- 岗位卡展示“匹配结论、缺失数据、截止日期、当前流程”；
- 分数改为可解释结论，例如“暂无法评估：未识别到岗位要求”。

## 5.4 岗位详情

### 明确缺陷

1. 0 项要求时错误显示“硬门槛通过”。
2. 数据无效时仍开放材料和申请主操作。
3. 应优先执行的 JD 检查和重新分析藏在“更多分析操作”中。

### 体验风险

1. “确定性匹配分”“Agent 语义分析”“确定性重算”是实现术语。
2. 多个分析动作没有顺序、依赖和结果关系。
3. 版本历史直接显示哈希，对普通用户无价值。
4. 页面没有明确回答“是否值得投、为什么、下一步是什么”。

### 建议优化

岗位详情首屏应按以下结构：

```text
决策：建议继续 / 谨慎 / 暂不建议 / 尚无法判断
关键原因：3 条以内
缺失信息：如有
主操作：补全岗位 / 选择简历方向 / 准备材料 / 建立申请
匹配依据：折叠展开
技术与版本记录：高级详情
```

## 5.5 材料列表

### 明确缺陷

1. Agent 候选按钮禁用但无原因。
2. 已有 Final 材料时仍突出重新创建和重新生成。
3. 用户从岗位详情进入后，没有进入针对该岗位的连续向导。

### 体验风险

1. Agent 生成和本地确定性生成作为并列产品能力。
2. “事实绑定草稿”“来源简历系列”“工作流 v2”偏内部语言。
3. “新建基础系列”默认选中，但用户当前任务可能是岗位定制材料。

### 建议优化

根据入口上下文决定页面模式：

- 从岗位进入：显示该岗位材料流程；
- 从导航进入：显示材料库；
- 已有可用 Final：优先“预览、下载、用于申请”；
- 需要新建时采用“选择方向 → 生成 → 检查 → 定稿”的单一流程。

## 5.6 材料详情

### 明确缺陷

1. Final 内容显示在可编辑外观的 textbox 中，但没有保存动作。
2. 只有姓名和两个技能的材料仍显示“复核未发现阻断问题”，审核结论可信度不足。
3. 主动作分散在页面不同位置。

### 体验风险

1. `basic`、`skill`、事实版本和哈希默认展示。
2. 版本血缘、父版本和不可变版本链压过材料正文。
3. “保存为可复用简历版本”的规则难以理解。

### 建议优化

- Final 正文使用只读预览，不使用编辑控件外观；
- 编辑必须产生明确的新草稿版本；
- 审核同时检查完整性，而不只是事实冲突；
- 首屏集中“预览、下载、用于申请、基于此修改”；
- 血缘和哈希进入审计详情。

## 5.7 申请列表

### 明确缺陷

1. 已有申请时仍长期展示创建表单。
2. 无可选岗位时按钮仍出现且无原因。
3. 卡片显示“事件流 v4”。

### 产品能力缺口

1. 缺少看板、列表和日历视图。
2. 卡片缺少下一事项、截止、风险、最近更新。
3. 无法快速筛选长期无进展、待跟进、面试中等状态。

### 建议优化

- 默认显示管理视图，“跟踪新岗位”使用明确按钮打开表单；
- 卡片首先展示公司、岗位、当前阶段、下一步和最近变化；
- 增加按阶段看板，列表和日历可后续迭代。

## 5.8 申请总览

### 明确缺陷

1. 系统事件备注仍出现英文：
   - `Final material is available.`
   - `Application materials must be prepared before submission.`
   - `Application tracking created from the selected job post.`
2. 同一分钟内出现多次状态变化，但页面没有解释这些是系统初始化事件。

### 体验风险

1. “申请总览”和“申请进度”的职责仍需学习后才能区分。
2. “记录关联正常”“数据关联检查”是系统健康概念，不是求职进展。
3. 每日简报和每周简报只复述数量，没有洞察和行动建议。
4. 搜索岗位、申请、材料和任务放在总览中，价值不明确。

### 建议优化

- “申请总览”重命名为“求职进展”；
- “申请进度”重命名为“申请列表”或“全部申请”；
- 数据关联检查移入高级诊断，异常时才在总览提示；
- 简报给出变化、风险和建议，而不是重复计数。

## 5.9 申请详情

### 明确缺陷

1. 两套近似进度录入并列。
2. `0 待确认`区域仍展示已确认候选。
3. “继续推进这份申请”只提供固定链接，不根据当前状态推荐动作。
4. 时间线序号容易与版本号混淆。
5. 归档缺少后果说明和清晰二次确认。

### 产品能力缺口

1. 缺少联系人、申请编号、下次行动、截止日期和风险。
2. 没有围绕当前阶段生成检查清单。
3. 邮件、手动输入、候选事件和正式事件关系仍复杂。

### 建议优化

申请详情应成为真正的申请工作台：

```text
当前阶段与下一步
关键时间和联系人
待确认邮件进度
任务与面试
已投材料
完整历史
```

历史审计信息默认折叠。

## 5.10 任务与日程

### 明确缺陷

1. 页面名称包含“日程”，但没有日历视图。
2. “新增真实任务”中的“真实”是内部边界语言。
3. “提前 1 小时 / 提前 1 天”实际是添加提醒，文案不准确。
4. 没有显示已设置提醒，可能重复添加。

### 体验风险

1. 每张卡片暴露完成、两个提醒、延后、取消五个动作。
2. 自动生成的岗位截止任务也可随意完成、延后和取消。
3. 没有按今天、逾期、未来或申请分组。

### 建议优化

- 页面改为“任务”或补充日历；
- 主动作只保留“完成”和“更多”；
- 提醒统一为“设置提醒”；
- 自动任务区分“来源时间”和“个人提醒时间”；
- 默认按“逾期、今天、未来”分组。

## 5.11 面试中心

### 明确缺陷

1. 创建面试默认时间是打开页面的当前时间，不是合理的未来时间。
2. 导航“面试准备”和 H1“面试中心”不一致。
3. 无面试时直接展示创建表单，没有说明自动产生路径。

### 体验风险

1. 申请尚未进入面试阶段也可直接创建面试。
2. “准备包只读取冻结版本……”是架构说明。
3. 0 状态的复盘建议和长期改进项占用空间。

### 建议优化

- 默认时间改为下一个合理时段，或不预填；
- 优先从已确认面试进度创建；
- 手动创建作为补充入口；
- 首屏回答“下一场面试是什么、还要准备什么”。

## 5.12 招聘邮件

### 明确缺陷

1. 显示原始英文状态 `unconfigured`。
2. 邮箱配置与邮件使用混合。
3. 两个测试按钮的区别不清晰。
4. 保存、测试、同步的步骤关系只靠按钮禁用表达。
5. 设置和业务页存在重复邮箱入口。

### 体验风险

1. IMAP、TLS、UID SEARCH、BODY.PEEK、Header 等术语密度高。
2. 只读安全边界说明过长，压过用户任务。
3. “每 10 分钟”没有配套最近结果和下一次同步。

### 建议优化

- 将完整配置移入设置；
- 日常页显示账户摘要和“管理连接”；
- 邮件结果以申请聚合，而不是以协议和扫描记录为中心；
- 测试统一为“保存并验证连接”；
- 同步状态明确区分未配置、未验证、可用、同步中、失败。

## 5.13 全部待确认

### 明确缺陷

1. 导航“全部待确认”、页面“审查中心”、eyebrow“待我确认”三套名称。
2. 统一中心不能就地处理，仍要求进入业务模块。
3. 空状态使用审计语言而不是行动指导。

### 产品能力缺口

1. 未按紧急程度、类型和影响排序。
2. 不能精确定位到实体和字段。
3. 缺少批量确认的安全边界说明。

### 建议优化

统一名称为“待我确认”，并尽量支持就地：

- 查看证据；
- 修改关键字段；
- 确认或拒绝；
- 查看影响；
- 跳转到具体业务实体。

## 5.14 职业档案

### 已改善

- 档案主页面已经减少默认技术元数据；
- 导入、确认、准备材料三个入口关系比旧版清晰；
- 来源和记录已折叠。

### 体验风险

1. 没有明确档案完整度或“已经足够用于哪些功能”。
2. 已确认事实缺少直接编辑/纠错的清晰入口。
3. “已拒绝 1，保留审计记录”对普通用户价值较低。
4. 求职偏好、优势洞察和每周策略仍被归入一个模糊的可选区域。
5. “手动添加一段经历”与事实类别、确认流程关系不明确。

### 产品能力缺口

1. 缺少档案维护时间线和变更摘要。
2. 缺少“哪些内容被岗位匹配和材料使用”的影响说明。
3. 缺少用户可理解的档案健康状态：
   - 基本可用；
   - 教育缺失；
   - 项目证据不足；
   - 最近 90 天未维护等。

## 5.15 资料导入与事实确认

### 已改善

- 文件选择入口已中文化；
- 技术导入元数据已折叠；
- 导入后说明了需要用户确认。

### 体验风险

1. 文件上传和粘贴文本仍是两个平铺区块，没有推荐入口。
2. 已导入文档只显示提取数量，不显示确认进度。
3. 导入成功后缺少强主按钮“确认提取结果”。
4. “简历内容确认”放在“消息与确认”导航中，而不是“我的资料”，用户难以形成位置记忆。
5. 待确认为空时流程清晰，但已处理历史和纠错入口不足。

### 建议优化

资料导入后应直接进入：

```text
导入完成
→ 提取了 N 条内容
→ 发现 M 条可能重复/冲突
→ [开始确认]
```

“简历内容确认”应成为职业档案内部流程，统一待确认中心只做跨业务聚合入口。

## 5.16 设置：基础设置

### 明确缺陷

1. 时区使用自由文本，用户可以输入无效值。
2. 日期格式示例固定为 `2026-07-26`，与当前日期不一致，像测试残留。
3. “测试常规设置”“测试外观设置”“测试安全设置”没有明确用户价值。
4. 全局“配置已生效”不能表达当前表单是否有未保存修改。
5. 保存按钮位于长页面底部，不易发现。

### 体验风险

1. 默认展示数据库、配置、备份、导出完整绝对路径。
2. 工作区实际路径仍包含 `nanobot-career`，与产品品牌目标冲突。
3. 基础设置更像文件系统状态页，而不是用户偏好设置。

说明：路径中的旧项目名是实际部署目录问题，不能只替换前端文案。正式交付前应提供目录迁移方案。

## 5.17 设置：AI 服务

### 体验风险

1. 供应商列表非常长，Custom 默认选中。
2. 页面显示“0 个”，但新增区缺少明显的分步表单层级。
3. 七类任务映射虽然折叠，但出现多个相同“未配置”下拉项，用户不知道分别对应什么。
4. 用户无法判断哪些功能必须配置 AI、哪些可本地完成。
5. 最近测试为空时没有引导先完成哪个动作。

### 建议优化

- 默认优先展示常用服务商；
- Custom 放到列表末尾；
- 使用“选择服务商 → 填写密钥 → 选择模型 → 保存并测试”向导；
- 任务映射提供“全部使用默认模型”；
- 每项能力说明 AI 缺失时的降级行为。

## 5.18 设置：招聘与邮箱

### 明确缺陷

1. 分类名称是“招聘与邮箱”，页面实际只配置 OpenCLI。
2. “配置牛客与 BOSS”和“配置只读邮箱”跳到其他业务页面。
3. 设置中心没有形成配置闭环。

### 体验风险

1. OpenCLI 可执行文件路径是工程配置，不应成为普通用户主要输入。
2. “外部 OpenCLI”“独立配置表单”等描述以系统架构为中心。

### 建议优化

设置页直接承载三个来源卡：

```text
牛客招聘信息
BOSS 手动搜索
只读招聘邮箱
```

OpenCLI 作为前两者的依赖状态显示在卡片高级详情中。

## 5.19 设置：消息通知

### 明确缺陷

1. 未配置时“删除配置”仍显示为普通可用按钮。
2. 测试按钮禁用原因依赖一整段说明。

### 体验风险

1. QQ App ID、App Secret、目标 ID 对普通用户门槛很高。
2. 通知目标格式需要用户理解个人/群 OpenID。
3. 事件订阅、免打扰和凭据一次性全部展开。

### 建议优化

- QQ 明确标记为高级通知渠道；
- 未配置时不显示删除；
- 先完成连接，再配置事件和免打扰；
- 提供通知目标验证；
- 测试失败应指明是凭据、目标还是网络问题。

## 5.20 设置：定时任务

### 明确缺陷

1. 按钮仍叫“保存 Scheduler”，中英文和用户术语不统一。
2. 最近自动任务显示 50 条一分钟一次的空运行。
3. “邮箱与招聘数据源”把两个不同来源合并为一个开关。

### 体验风险

1. “检查周期（秒）”是工程参数。
2. “向 Channel 分发通知”是实现术语。
3. 用户不知道关闭总服务会影响哪些业务。

### 建议优化

普通模式只配置：

- 任务提醒；
- 招聘信息每日获取；
- 邮箱同步；
- 每日档案整理；
- 通知发送。

轮询秒数、租约和运行明细进入高级设置。

## 5.21 设置：备份与迁移

### 已改善

- 已明确普通迁移包不包含凭据；
- 支持加密后包含凭据；
- 风险操作已有分区。

### 体验风险

1. 文件控件按钮只显示“迁移包”，不像明确的“选择迁移包”。
2. 恢复父目录仍需要手工输入路径。
3. 导入前没有展示包摘要、版本和目标影响。
4. “测试导出条件”“测试导入入口”与真正的校验动作边界不清。
5. “清理未引用文件”对普通用户难以判断是否安全。

### 建议优化

- 使用系统文件和文件夹选择器；
- 选择包后先展示只读检查结果；
- 明确导入创建新工作区，不覆盖当前工作区；
- 清理操作展示将删除的数量和可恢复性；
- 测试功能并入导入/导出前置校验，不单独暴露。

## 5.22 设置：高级设置与诊断页

### 明确缺陷

1. Agent 运行记录显示 `succeeded`、`profile_fact_extraction` 等英文内部值。
2. 申请总览中的系统备注仍有英文，而代码中已有部分翻译映射，说明映射未覆盖或数据呈现路径不一致。

### 体验风险

1. 日志级别、审计保留、后台任务租约对普通用户风险较高。
2. “重新进入初始化向导”没有说明是否影响现有配置和服务。
3. 服务状态再次展示全部磁盘路径。
4. Agent 记录强调 Schema、Prompt、Token、哈希，但缺少“这次功能是否对用户结果产生影响”。
5. 后台任务页只有“队列已就绪”，缺少历史失败和恢复操作，诊断价值有限。

### 建议优化

- 高级设置增加风险说明和恢复默认值；
- 技术枚举全部中文化；
- 诊断页以“影响、原因、处理动作”为第一层；
- 完整 Schema、哈希和版本仅在展开详情显示。

## 6. 跨产品系统性问题

## 6.1 推荐逻辑不可信

首页、岗位、申请详情都没有基于完整业务状态推荐下一步。各页面使用局部条件或固定链接，导致建议与真实状态脱节。

需要建立统一的“Next Best Action”应用服务，而不是继续在各 React 页面中写独立条件。

## 6.2 配置状态、可用状态和运行状态混淆

当前常见状态包括：

- 已配置；
- 已启用；
- 测试通过；
- 可连接；
- 自动运行；
- 最近成功；
- 当前受阻。

这些状态在不同模块的表达不一致。建议统一为：

| 状态 | 用户含义 |
|---|---|
| 未配置 | 缺少必要信息 |
| 待验证 | 已保存但未测试 |
| 可用 | 测试通过，可手动使用 |
| 已自动运行 | 自动任务已开启 |
| 受阻 | 缺少上游依赖 |
| 异常 | 最近测试或运行失败 |
| 需重启 | 已保存但当前进程未应用 |

## 6.3 配置和使用场景混杂

邮箱是最明显的问题，牛客/BOSS 也存在同类问题：

- 设置页只提供跳转；
- 业务页承担完整配置；
- 用户无法建立“在哪里配置、在哪里使用”的稳定认知。

原则应是：

> 设置中心负责连接和规则；业务页面负责内容、状态和行动。

## 6.4 两套执行路径并列

Agent、本地规则、确定性计算、候选、正式等设计对内部可靠性有价值，但不应全部成为同等级按钮。

前端应呈现单一业务目标，运行策略由应用层选择，失败时再提供替代方案。

## 6.5 空状态缺少可执行 CTA

多个空状态只解释“为什么为空”，没有直接操作：

- 每日招聘；
- 待确认；
- 邮件同步；
- Agent 测试记录；
- 面试；
- 后台任务。

空状态至少应回答：

```text
这里会出现什么
为什么现在没有
用户现在能做什么
```

## 6.6 禁用控件没有原因

这是当前最普遍、最容易让用户认为“按钮坏了”的问题。应建立统一的 `BlockedAction` 组件和依赖描述，不允许业务页面直接渲染无解释的 disabled 主按钮。

## 6.7 用户术语不统一

当前仍存在：

| 导航/入口 | 页面或内容 |
|---|---|
| 今日招聘 | 每日招聘 |
| 收藏的岗位 | 目标岗位 |
| 申请总览 | 求职总览含义 |
| 面试准备 | 面试中心 |
| 全部待确认 | 审查中心 / 待我确认 |
| 招聘邮件 | 消息中心 |
| 自动任务 | Scheduler |

建议建立产品词汇表，并在前端测试中检查关键页面标题。

## 6.8 审计信息默认过多

产品希望具备工业级可追溯性是正确的，但“保存审计信息”和“默认展示审计信息”是两件事。

推荐三层信息：

```text
用户层：结论、状态、时间、来源
解释层：依据、影响、为什么
审计层：版本、哈希、Schema、技术事件
```

当前多个页面把三层同时展开。

## 6.9 状态模型未形成用户可理解的视图

系统有丰富的内部状态和事件，但用户层缺少：

- 当前能不能继续；
- 还差什么；
- 最近发生了什么；
- 哪个信息是系统推测；
- 哪个已经由用户确认；
- 操作会改变什么。

应为 Job、Material、Application、DataSource 提供专门的用户状态 ViewModel。

## 6.10 移动端只是“变成单列”，还不是移动任务流

当前 CSS 已完成：

- 抽屉导航；
- 设置分类下拉；
- 多列布局转单列；
- 表单和详情区域转单列。

但单列化会带来新的问题：

- 申请详情的两个进度表单会连续占据多个屏幕；
- 材料详情的审计信息会把主操作推到很远；
- 每个任务的五个按钮会形成密集操作区；
- 长设置表单保存按钮远离修改位置；
- 首屏无法同时看到状态、阻塞原因和主操作；
- 抽屉中仍有多级导航，进入深层页面后切换成本高。

移动端需要额外设计：

- 底部或吸顶主操作；
- 详情页分段导航；
- 次要信息折叠；
- 更多操作菜单；
- 表单分步；
- 返回上一个业务对象的明确入口。

## 7. 问题严重度矩阵

### P0：可能形成错误正式记录或数据风险

| 编号 | 问题 | 类型 |
|---|---|---|
| P0-01 | 岗位要求为 0 时显示硬门槛通过 | 明确缺陷 |
| P0-02 | 无有效岗位分析仍开放生成材料和创建申请 | 明确缺陷 |
| P0-03 | 材料审核只检查阻断冲突，明显不完整内容仍可显示通过 | 产品能力缺口 |

### P1：阻断核心任务或明显错误引导

| 编号 | 问题 | 类型 |
|---|---|---|
| P1-01 | 首页建议与真实业务状态不一致 | 明确缺陷 |
| P1-02 | Agent 与确定性两套流程直接交给用户 | 体验风险 |
| P1-03 | 邮箱配置和业务使用混在一起 | 明确缺陷 |
| P1-04 | 禁用主按钮无原因和修复入口 | 明确缺陷 |
| P1-05 | 申请详情两套近似进度录入 | 明确缺陷 |
| P1-06 | 待确认数量与列表内容矛盾 | 明确缺陷 |
| P1-07 | 申请下一步是固定链接，不根据阶段判断 | 产品能力缺口 |
| P1-08 | 数据来源设置无法在设置中心闭环 | 明确缺陷 |
| P1-09 | 导入资料后缺少直接事实确认主路径 | 体验风险 |
| P1-10 | 数据来源空状态无法区分未同步与确实无结果 | 产品能力缺口 |

### P2：高频理解成本和操作负担

| 编号 | 问题 | 类型 |
|---|---|---|
| P2-01 | 版本、哈希、事件流和 Schema 默认暴露 | 体验风险 |
| P2-02 | 申请列表创建与管理混排 | 体验风险 |
| P2-03 | 任务操作密度过高 | 体验风险 |
| P2-04 | 自动任务被 50 条空跑记录淹没 | 明确缺陷 |
| P2-05 | 设置一级分类和系统路径信息过多 | 体验风险 |
| P2-06 | 无意义的外观/常规配置测试按钮 | 建议优化 |
| P2-07 | 产品术语多处不一致 | 明确缺陷 |
| P2-08 | 申请总览与申请进度职责不够明确 | 体验风险 |
| P2-09 | 移动端详情和表单过长 | 体验风险 |
| P2-10 | 英文系统事件和枚举未完整翻译 | 明确缺陷 |

### P3：一致性和完成度

| 编号 | 问题 | 类型 |
|---|---|---|
| P3-01 | 目标岗位仍出现原生英文文件按钮 | 明确缺陷 |
| P3-02 | 日期格式示例是固定历史日期 | 建议优化 |
| P3-03 | 当前工作区默认展示全部内部路径 | 建议优化 |
| P3-04 | 服务状态重复展示文件系统结构 | 建议优化 |
| P3-05 | QQ 未配置时仍显示删除配置 | 明确缺陷 |
| P3-06 | 备份文件控件和校验文案不直观 | 建议优化 |
| P3-07 | 面试默认当前时间 | 明确缺陷 |
| P3-08 | 页面空状态仍保留大量零值卡片 | 建议优化 |

## 8. 建议的目标产品体验

## 8.1 首页

首页只回答三个问题：

```text
我今天最应该做什么？
有哪些有时限或需要确认的事项？
如果今天没有紧急事项，我可以推进什么？
```

不要同时承担系统状态、配置入口、业务分析和所有模块摘要。

## 8.2 岗位

岗位详情只回答：

```text
这个岗位的信息完整吗？
我是否值得继续？
依据是什么？
下一步准备什么？
```

“使用哪种分析引擎”不是用户问题。

## 8.3 材料

材料流程应是：

```text
选择岗位
→ 选择修改方向
→ 生成草稿
→ 检查事实与完整性
→ 用户修改
→ 定稿
→ 下载或用于申请
```

材料库和岗位定制流程应区分页面模式。

## 8.4 申请

申请详情应成为整个产品的核心业务页：

```text
状态
→ 下一步
→ 截止和联系人
→ 待确认邮件
→ 任务和面试
→ 已投材料
→ 历史记录
```

用户不应该在申请详情、任务、邮件和审查中心之间反复寻找同一申请的信息。

## 8.5 设置

设置中心负责：

```text
连接
规则
验证
自动化
数据安全
```

业务页面负责：

```text
内容
状态
行动
结果
```

不要让用户在招聘邮件页配置 IMAP，也不要让设置页只提供跳转到业务页的按钮。

## 9. 分阶段整改计划

## 第一阶段：修复错误判断和流程保护

目标：不再错误引导用户产生低质量正式记录。

任务：

1. 修复“0 项岗位要求 = 硬门槛通过”；
2. 岗位数据不完整时阻止或降级材料/申请主操作；
3. 首页下一步算法接入档案、岗位、材料和申请状态；
4. 材料审核增加最低完整性检查；
5. 申请候选数量只统计并只展示待确认项；
6. 修复面试默认当前时间；
7. 补齐英文系统事件和状态映射。

验收：

- 不完整 JD 不会显示通过；
- 用户已有申请时首页不会默认建议导入经历；
- 已处理候选不出现在待确认区域；
- 明显空材料不能显示无阻断通过。

## 第二阶段：统一行动、阻塞和状态模型

目标：每个页面都能明确告诉用户当前状态和下一步。

任务：

1. 建立统一 `ActionAvailability` 模型；
2. 建立 `BlockedAction` 展示规范；
3. 为岗位、材料、申请和数据来源提供用户状态 ViewModel；
4. 所有 disabled 主操作补充原因和修复入口；
5. 统一配置、验证、可用、启用、异常状态；
6. 将 Agent/确定性执行策略隐藏到应用层。

验收：

- 用户点击或查看任何不可用操作都能知道原因；
- 普通页面不要求用户选择 Agent 或确定性流程；
- 数据来源状态在设置、首页和业务页含义一致。

## 第三阶段：重构申请工作台和连续业务流

目标：从岗位到申请推进形成连续流程。

任务：

1. 岗位详情加入决策结论和推荐步骤；
2. 从岗位进入材料定制向导；
3. Final 后直接进入创建/绑定申请；
4. 申请详情统一进度更新入口；
5. 聚合联系人、截止、任务、面试、邮件和材料；
6. 待确认内容支持在申请详情就地处理；
7. 统一审查中心精确定位具体实体。

验收：

```text
岗位
→ 分析
→ 材料
→ 投递
→ 邮件确认
→ 任务/面试
```

全程不需要用户理解候选事件、事实版本、事件流或哈希。

## 第四阶段：配置中心职责重构

目标：所有连接类配置在设置内完成并可以验证。

任务：

1. 将牛客、BOSS、邮箱配置纳入设置“数据来源”；
2. 招聘邮件页只保留日常业务；
3. AI 服务改为分步“保存并测试”；
4. QQ 改为连接后再配置订阅；
5. 自动任务按用户业务拆分；
6. 删除外观、日期格式等无意义测试按钮；
7. 测试动作并入真正需要验证的连接和路径；
8. 增加未保存修改状态和吸底/吸顶保存栏。

验收：

- 用户不用离开设置即可完成全部连接；
- 每个需要测试的配置都有明确测试目标和结果；
- 不需要测试的偏好设置不再出现测试按钮；
- 修改未保存时不会显示“配置已生效”。

## 第五阶段：信息降噪和术语统一

目标：业务页面不再像工程控制台。

任务：

1. 建立并执行产品词汇表；
2. 统一导航、H1、eyebrow 和按钮名称；
3. 版本、哈希、Schema、Prompt、内部枚举移入审计详情；
4. 删除“真实任务、事件流 v4、保存 Scheduler”等内部文案；
5. 空状态根据原因提供 CTA；
6. 自动任务空跑记录聚合；
7. 默认只展示工作区根目录。

验收：

- 普通用户主流程不出现 Schema、Prompt、hash、lease、workflow version；
- 同一业务对象全产品使用同一个名称；
- 空状态均有明确原因和下一步。

## 第六阶段：移动端专项改造与验收

目标：移动端不只是桌面页面单列化。

任务：

1. 申请、岗位、材料详情增加吸顶/吸底主操作；
2. 长详情增加分段导航；
3. 次要动作收入“更多”；
4. 创建表单改为抽屉、弹层或分步流程；
5. 设置保存栏保持可见；
6. 任务按钮改为主动作加菜单；
7. 使用真实 390px、430px 视口逐页测试。

验收：

- 核心任务在移动端不需要连续滚动多个屏幕后才能执行；
- 主操作始终容易找到；
- 不发生横向溢出；
- 抽屉导航、返回和焦点顺序可用；
- 触控目标符合移动端尺寸要求。

## 10. 整改验收标准

### 10.1 用户任务成功标准

以下任务应由不了解 CareerConsole 内部模型的用户独立完成：

1. 初次建立职业档案；
2. 获取今天的牛客招聘信息；
3. 导入一个具体 JD；
4. 判断岗位是否值得申请；
5. 选择简历修改方向；
6. 生成、修改并定稿材料；
7. 创建申请并确认已投递；
8. 从邮件中确认申请进度；
9. 创建任务和面试准备；
10. 导出和导入工作区。

每个任务都应满足：

- 用户知道从哪里开始；
- 页面只有一个明确主操作；
- 不可用时知道缺什么；
- 完成后知道结果；
- 知道下一步；
- 不需要理解内部 ID、Schema、哈希、事件流或执行引擎。

### 10.2 一致性标准

- 导航名、页面标题和按钮术语一致；
- 同一种状态在所有页面颜色和文案一致；
- 所有时间明确使用北京时间；
- 所有英文内部枚举均映射为中文；
- 所有危险操作说明影响并要求二次确认；
- 所有配置状态区分已保存、已验证、已启用和最近运行。

### 10.3 可用性标准

- 不存在无解释的 disabled 主按钮；
- 不存在没有 CTA 的阻断型空状态；
- 页面主操作首屏可见或保持吸附；
- 用户修改设置后立即看到“未保存”状态；
- 保存和测试反馈靠近对应配置；
- 失败消息包含原因、影响和可执行修复动作。

### 10.4 响应式与可访问性标准

- 390×844、430×932、768×1024、1366×768、1920×1080 均完成真实浏览器验收；
- 键盘可访问所有主要操作；
- 禁用原因不依赖 hover；
- 抽屉打开后焦点被限制在抽屉内，关闭后返回触发按钮；
- 表单错误与字段建立可访问关联；
- 减少动效设置真实影响所有非必要动画；
- 页面缩放到 200% 仍可完成核心任务。

## 11. 不建议改动的核心设计

本报告不建议因为前端体验问题而删除以下能力：

- AgentLoop 及任务运行能力；
- Cron/Scheduler 定时基础设施；
- Channels 通知出口；
- 通用工具类；
- 职业事实人工确认；
- 邮件只读和最长一个月扫描边界；
- 牛客只自动获取当天、历史由用户主动选择；
- OpenCLI 作为外部应用使用；
- 岗位和材料版本化；
- 申请事件流；
- Agent 结果先成为候选、由用户确认后进入正式数据；
- 审计和可追溯能力。

需要改变的是这些能力在用户界面的暴露方式、默认顺序和状态解释。

## 12. 推荐下一步

下一轮不应从配色、卡片圆角或局部文案开始。建议先完成：

1. 岗位无有效要求时的流程保护；
2. 首页推荐算法；
3. 统一禁用原因和阻塞模型；
4. 隐藏 Agent/确定性双流程；
5. 申请详情统一进度入口；
6. 邮箱配置迁移到设置；
7. 配置状态模型；
8. 内部术语和审计信息降级。

这八项完成后，再做视觉和移动端专项优化，才能避免仅仅把复杂流程“设计得更漂亮”，但没有降低用户完成任务的难度。

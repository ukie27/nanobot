# CareerConsole 用户体验、配置与部署审查报告

> 审查日期：2026-07-27  
> 审查范围：部署/启动、首次初始化、全部配置、日常核心业务页面、业务模块衔接  
> 本报告只提出前端信息架构、交互、配置、初始化、部署和业务衔接方面的改进，不建议重做现有核心业务模型。

## 1. 结论摘要

CareerConsole 的业务底座已经能够形成完整闭环。实际隔离环境验证已走通：

```text
简历文本导入
→ 生成候选事实
→ 人工确认事实
→ 导入具体 JD
→ 本地岗位匹配
→ 生成并审核申请材料
→ Final PDF
→ 创建申请跟踪
→ 创建关联任务
→ 创建待审申请进度
→ 统一审查队列
→ Dashboard 与求职总览同步更新
```

这说明当前阶段不需要大幅改动职业档案、岗位、材料、申请、任务、审查等核心领域逻辑。主要问题集中在产品外壳：

1. **首次使用缺少稳定、可恢复的桌面产品启动边界。** 当前源码模式可以一条命令启动，但工作区切换使用进程原位替换；一旦新工作区不可写，旧服务先退出、新服务启动失败，用户会直接失去 UI。
2. **初始化向导更像多个设置页面的拼接，而不是明确的启用决策流程。** “保存状态并继续”并不保存嵌入表单；可选能力没有清晰的“启用/暂不启用”状态；最终由系统根据 capability 自动推断跳过项。
3. **配置状态与真实可用能力混淆。** Scheduler 默认启用且所有子能力默认打开；七类 Agent 任务默认启用但没有 Provider。页面会同时呈现“已启用”和“实际上不能运行”。
4. **主导航没有围绕用户的每日求职路径组织。** 核心的“全链路工作区”被放进“高级与诊断”；“今日”和“求职总览”职责重叠；“招聘信息/招聘机会/岗位库”概念没有用用户语言解释。
5. **业务模块底层关联正确，但前端缺少连续任务流。** 数据能正确联动，用户却需要理解多个内部对象并在页面间自行跳转。
6. **普通设置与工程运维参数混在一起。** revision、Schema、Provider、Agent Routing、lease、Connector、UIDVALIDITY 等术语大量直接暴露给普通用户。
7. **现有文档之间存在版本矛盾。** README 说明普通用户只需 `serve`，Windows 安装文档仍要求先执行 `workspace create/setup/doctor`；迁移功能已存在，但部分文档仍称其为未来能力。

建议下一阶段以“产品外壳重构”为目标，保持领域服务和数据库核心逻辑不变，依次完成：

```text
安全启动与工作区切换
→ 初始化状态机
→ 配置能力模型
→ 主导航和页面职责
→ 跨模块任务流
→ 文案与错误恢复
→ Windows 交付形态
```

## 2. 审查方式与限制

### 2.1 实际运行验证

本次使用隔离环境启动真实 CareerConsole 服务：

```text
服务地址：http://127.0.0.1:8876
Bootstrap 隔离目录：
D:\project\job-agent\nanobot-career\.career-console\ux-review-20260727

正式测试工作区：
D:\tmp\CareerConsoleUXReviewParent\CareerConsole

数据库 revision：
20260726_0026
```

已实际验证：

- 无正式工作区时执行单条 `serve` 命令；
- Bootstrap 临时工作区创建和数据库迁移；
- `/health/live` 与 `/health/ready`；
- 正式工作区校验、创建、激活、重启；
- 初始化完成前后的 Bootstrap/Product 模式；
- 默认 Provider、Agent、OpenCLI、邮箱、QQ、Scheduler 状态；
- 所有主要业务页面依赖的 API 空状态；
- 一条完整样例业务链路；
- Dashboard、审查队列、申请总览的跨模块数据更新。

### 2.2 浏览器限制

本次会话没有可用的 Browser 实例，因此没有进行真实鼠标点击、键盘输入、视觉截图和响应式布局检查。前端体验结论来自：

- 真实服务和 API 请求；
- 前端 React 代码；
- 已构建前端产物；
- 服务日志；
- 按用户路径逐步核对页面行为。

因此，本报告可以确认流程、状态、链接、接口和信息架构问题，但视觉层级、控件尺寸、焦点顺序、键盘可访问性、不同分辨率表现需要在后续前端改造时补做浏览器验收。

### 2.3 未执行的外部连接

为避免影响真实账户，本次隔离工作区没有连接用户邮箱、QQ 或执行真实招聘网站同步。邮箱和外部数据源部分依据其真实配置 API、页面实现和现有运行状态进行评估。

## 3. 前端 Skill 安装结果

已安装前端设计 Skill：

```text
名称：frontend-design
来源：anthropics/skills
安装位置：C:\Users\wq_li\.codex\skills\frontend-design
```

该 Skill 从下一轮会话起可用于前端信息架构、视觉层级、组件设计和页面改造。官方 OpenAI Skill 仓库中没有对应的 `frontend-skill` 安装路径，因此采用了当前可安装的前端设计 Skill。

## 4. 当前用户旅程

### 4.1 当前首次使用路径

```text
获取源码和 Python 环境
→ 执行 python -m career_console serve
→ 浏览器进入 Bootstrap 初始化
→ 选择父目录
→ 验证目录
→ 创建正式工作区
→ 服务进程原位重启
→ 继续配置 AI/档案/数据源/通知/自动任务
→ 完成初始化
→ 服务再次原位重启
→ 进入 Product 模式
```

从产品意图上，这条路径是合理的：普通用户不应先学习 CLI 工作区命令。但实现上还没有达到“安装后稳定打开应用”的体验，主要原因是当前仍以源码运行、浏览器 UI 和后端进程重启为中心。

### 4.2 当前日常路径

当前页面大致分为：

```text
今日
招聘信息 → 岗位库
申请看板 → 任务与日程 → 面试
职业档案 → 资料导入 → 申请材料
招聘邮件 → 待确认事项
设置
高级与诊断
```

真实用户的目标路径更接近：

```text
今天需要做什么
→ 有哪些新机会
→ 哪些岗位值得投
→ 针对岗位准备什么材料
→ 我已经投到哪一步
→ 有哪些邮件、面试和截止时间
→ 有哪些内容需要我确认
```

当前系统的数据模型支持这条路径，但导航和页面命名仍以内部模块为主。

## 5. 实际业务闭环验证

本次在隔离工作区创建了完整样例数据。

### 5.1 验证结果

| 环节 | 实际结果 |
|---|---|
| 简历文本导入 | HTTP 201，生成 16 条候选事实 |
| 事实确认 | 16 条候选事实全部确认 |
| JD 导入 | HTTP 201，提取 5 条岗位要求 |
| 本地岗位分析 | 匹配分 100，5 条匹配、0 条缺口 |
| 材料生成 | 生成 16 个事实绑定内容块，Reviewer 0 错误 |
| 材料 Final | 成功生成 Final 和 PDF 导出 |
| 创建申请 | 申请自动进入 `ready_to_apply`，识别到 1 份可用 Final 材料 |
| 创建任务 | 正确关联 Application ID 和 Job Post ID |
| 创建进度提案 | 成功创建 open 的人工审查任务 |
| 统一审查队列 | 能看到该申请进度提案 |
| 求职总览 | 申请总数 1、待审事项 1，最近状态变化 2 条 |
| Dashboard | 待审事项 1、未来任务 1 |

### 5.2 已经正确的核心业务关联

以下关联值得保留：

- 确认后的职业事实才进入岗位匹配和材料生成；
- 岗位导入时自动建立版本和确定性分析；
- 材料内容块绑定事实快照；
- Final 材料会影响申请的可投递状态；
- 申请事件使用版本化时间线；
- 邮件和 Agent 只创建候选事件，不直接修改正式申请进度；
- 任务可以同时关联申请和岗位；
- 统一审查队列、Dashboard 和工作区总览共享业务状态；
- 北京时间边界已经贯穿任务、面试、邮件和招聘信息。

### 5.3 主要衔接问题

底层关联存在，但前端缺少“下一步”引导：

- 导入简历后只显示“提取了 N 条待审查事实”，没有直接进入事实审查的主按钮；
- `/review` 事实审查没有主导航入口；
- 招聘机会到具体 JD 的关系需要用户自己理解“机会池”和“岗位池”；
- 岗位详情同时展示“加入申请跟踪、生成申请材料、规划简历方向、Agent 分析、确定性重算”，动作优先级不清晰；
- 申请创建后，材料、任务、邮件证据、进度提案分布在不同区域，缺少一个明确的申请操作时间线；
- 统一审查中心的卡片只跳到业务模块页面，不总能定位到具体待审对象；
- “今日”和“全链路工作区”都在做总览，但没有明确区分“行动入口”和“数据分析”。

## 6. 部署与启动审查

### 6.1 一条命令只对源码开发环境成立

实际执行：

```powershell
.\.venv\Scripts\python.exe -m career_console serve --port 8876
```

可以自动创建 Bootstrap 工作区、迁移数据库并加载前端。这对开发者是有效的。

但对普通用户还缺少：

- Python、虚拟环境和依赖安装；
- Node 构建产物的发布保证；
- Windows 快捷方式或安装包；
- 应用进程的正常退出和重启；
- 崩溃后的恢复入口；
- 日志打开和诊断包导出；
- 自动升级或至少版本升级说明。

因此当前应明确区分：

1. **源码开发启动**：`.venv\Scripts\python.exe -m career_console serve`；
2. **最终用户安装启动**：后续应提供 Windows 安装包/便携包和 `CareerConsole.exe`；
3. **高级运维 CLI**：`doctor/setup/workspace/status`。

### 6.2 工作区切换存在服务失联风险

当前 `/api/v1/system/restart` 最终执行：

```python
os.execv(
    sys.executable,
    [sys.executable, "-m", "career_console", *sys.argv[1:]],
)
```

本次实际发生：

1. 正式工作区目录被创建并激活；
2. UI 请求重启；
3. 原服务退出；
4. 新进程尝试写新工作区日志；
5. 日志目录不可写；
6. 新进程启动失败；
7. 原 UI 完全不可访问。

沙箱权限触发了本次错误，但同类问题在真实环境也可能由网络盘离线、目录 ACL、杀毒软件、同步盘锁定、磁盘只读或路径失效触发。

目标实现应改为：

```text
验证父目录
→ 创建候选工作区
→ 真实写入/删除探针
→ 打开数据库并执行 quick_check
→ 验证 config/logs/runtime 可写
→ 写 pending workspace switch
→ 由稳定 launcher 启动新实例并检查 ready
→ 新实例 ready 后再关闭旧实例
→ 失败则保留旧实例并显示恢复原因
```

后端业务进程不应自己承担“替换自身且保证恢复”的职责。应引入稳定的桌面 Launcher/进程管理层。

### 6.3 目录验证不足

`validate_parent()` 当前主要验证路径规则，没有进行完整的真实可写探针。实际表现是：

```text
第一次点击“验证目录” → 成功
第二次点击“创建并切换” → 才发现无法创建/写入
```

这会让用户误以为第一步已经通过。验证至少应检查：

- 父目录存在或可创建；
- 可创建临时子目录；
- 可创建、flush、fsync、rename、delete 文件；
- 目标 `CareerConsole` 目录不存在或是合法工作区；
- 路径不是源码目录、用户主目录或磁盘根目录；
- 可用磁盘空间达到最低要求；
- Windows 长路径和 Unicode 路径可用。

### 6.4 原生目录选择器的产品边界不清晰

当前后端使用 `tkinter` 打开 Windows 文件夹选择器。这只适用于“浏览器和后端进程运行在同一台有桌面的 Windows 机器”。

以下环境可能失败或阻塞：

- Windows Service；
- 无桌面会话；
- 容器；
- 远程 Linux；
- 浏览器访问远程 CareerConsole；
- 多用户服务。

如果 CareerConsole 明确是 Windows 本地桌面单用户应用，可以保留原生选择器，但应由桌面壳调用，而不是 FastAPI 进程直接打开 GUI。如果未来支持远程部署，则必须改为服务器路径选择/手动输入，不能让浏览器误以为在选择用户本机目录。

### 6.5 文档冲突

当前至少存在以下冲突：

- `README.md`：普通用户首次使用只执行 `serve`；
- `docs/operations/windows-install.md`：先执行 `workspace create`、`setup`、`doctor`、`serve`；
- `docs/operations/workspace.md`：导入导出属于未来 CC-5；
- 设置页：已经提供 CC-5 迁移包导入导出；
- 全链路工作区：仍提示“恢复必须停止服务后使用 CLI”。

需要确定唯一的当前发布流程，并由文档版本或自动化检查避免过期描述长期存在。

## 7. 初始化向导审查

### 7.1 当前向导实际是设置组件拼装

向导直接嵌入：

- `ProviderAgentConfiguration`
- `DocumentsPage`
- `DataSourceSettings`
- `DataSourcesPage`
- `MessageCenterPage`
- `ChannelSettings`
- `SchedulerSettings`

优点是复用代码；问题是每个组件都保留了完整设置页的信息密度和独立保存逻辑。结果是“步骤很多，但状态不明确”。

### 7.2 “保存状态并继续”文案不准确

页脚按钮写的是：

```text
保存状态并继续
```

但该按钮只刷新 query 并切换步骤，不会提交当前嵌入表单。用户如果填写了 Provider、邮箱或 Scheduler 表单但没有点击组件内部的“保存”，再点击页脚按钮，输入会丢失。

建议采用以下两种方式之一：

1. 每一步只有一个主表单，页脚“保存并继续”真正提交该步骤；
2. 保留独立保存按钮，但页脚只写“下一步”，并在离开前检测未保存修改。

初始化向导更适合第一种。

### 7.3 缺少明确的启用/跳过决策

当前最终完成时按 capability 自动生成跳过列表。用户没有在每一步明确选择：

```text
现在启用
暂不启用
稍后配置
```

这会造成：

- 用户不知道某一步是否已经完成；
- 填了一半的配置与主动跳过无法区分；
- 最终能力检查只显示“已配置/暂未配置”，没有解释影响；
- 重新进入向导时难以恢复用户的步骤决策。

建议建立持久化的 onboarding step state：

```text
not_started
in_progress
configured
skipped
failed_validation
```

每步保存后更新状态，最终完成只允许工作区已配置，其余步骤必须是 `configured` 或 `skipped`。

### 7.4 Provider 被描述为可选，但影响范围未解释

向导文案说 Provider 可选，同时又说它用于邮件分析、岗位匹配和简历生成。实际上系统还存在确定性本地岗位匹配和材料基线。

用户需要看到清晰的能力矩阵：

| 未配置 AI | 配置 AI |
|---|---|
| 手工档案与事实审查 | Agent 事实提取增强 |
| 本地规则岗位匹配 | Agent 语义岗位匹配 |
| 确定性材料基线 | 简历方向、Drafter、Reviewer |
| 手工邮件关联 | 邮件结构化分析 |

否则用户会误以为不配置 AI 整个产品无法使用，或反过来认为所有功能仍可正常运行。

### 7.5 职业档案步骤没有完成事实确认

“建立职业档案”步骤只嵌入资料导入页面。导入后生成候选事实，但事实审查位于 `/review`，且没有主导航入口。

初始化中更合理的流程是：

```text
导入简历
→ 显示提取摘要
→ 进入简化事实确认
→ 至少确认基本信息/教育/技能/求职偏好
→ 标记职业档案步骤已完成
```

用户也可以选择“只导入，稍后审查”，但这应是明确决策。

### 7.6 数据来源步骤过重

同一初始化步骤同时展示：

- OpenCLI 可执行文件；
- 牛客来源；
- BOSS 来源；
- 只读邮箱；
- 牛客登录按钮。

OpenCLI 是依赖，牛客/BOSS 是招聘数据源，邮箱是申请证据源，三者的业务目的不同。建议拆成两个步骤或二级卡片：

```text
招聘信息来源
- OpenCLI 环境
- 牛客每日新增
- BOSS 手动搜索

申请进度来源
- 只读邮箱
```

并按依赖关系展示状态：

```text
OpenCLI 未安装 → 牛客/BOSS 不可启用 → 提供安装/检测说明
OpenCLI 可用但未登录 → 提供打开登录和重新检测
邮箱已保存但未测试 → 标记“待验证”
邮箱测试通过但轮询关闭 → 标记“已配置，手动同步”
```

### 7.7 完成页的能力状态过于粗糙

当前 capability 是布尔值，不能表达：

- 已保存但未连接测试；
- 已连接但未启用；
- 依赖缺失；
- 已启用但 Provider 未映射；
- 配置需要重启；
- 最近运行失败。

建议统一为：

```text
not_configured
configured
ready
enabled
degraded
blocked
restart_required
```

完成页应展示“能力是否可用”，而不只是“是否存在配置”。

## 8. 设置与配置审查

### 8.1 普通设置和高级设置需要分层

当前“常规与工作区”同时包含：

- 语言、时区、日期格式；
- 外观；
- 日志级别；
- 日志保留天数；
- Agent 审计保留；
- 任务 lease；
- 文档大小；
- 隐私；
- 强制填写配置变更原因。

普通用户不应在主设置流中处理 lease、revision 和日志保留。建议分为：

```text
常规
- 语言与时间
- 启动行为
- 外观

数据与安全
- 工作区
- 备份/导入/导出
- 隐私

高级运行参数
- 日志
- 审计保留
- 租约
- 文档限制
```

“变更原因”可自动生成人类可读内容，只有高级审计场景才允许用户补充备注。

### 8.2 Provider 配置已改善，但仍偏工程化

当前新增 Provider 已自动生成内部 ID，不再要求用户填写 Provider ID，这是正确方向。

仍存在的问题：

- 用户需要自己理解 Provider 类型、API URL、默认模型、可选模型；
- 没有常见 Provider 预设；
- 保存 Provider 和测试连接是两个分离动作；
- 测试历史展示内部 `provider_id`，而不是显示名称；
- Provider 可“启用”但 API Key 缺失或测试失败；
- Provider 保存后，没有引导用户完成 Agent 任务映射。

建议提供：

```text
选择服务商
→ 自动填写默认 URL
→ 输入 API Key
→ 选择/检测模型
→ 保存并测试
→ 应用推荐任务配置
```

自定义 OpenAI-compatible 作为高级选项保留。

### 8.3 Agent 任务映射不适合作为首次配置主流程

七类任务分别选择 Provider 和模型是强大的高级能力，但普通用户无法判断：

- 哪些任务必须使用模型；
- Drafter 与 Reviewer 为什么要分开；
- 留空模型会发生什么；
- 启用但没有 Provider 是否有效；
- 哪种模型适合哪类任务。

建议初始化只提供三个预设：

```text
统一模型：所有任务使用同一 Provider 和默认模型
质量优先：分析/生成/复核使用推荐的不同模型
自定义：展开当前完整任务映射
```

系统必须禁止或明确标记“task.enabled=true 且 provider_id=null”的无效状态。

### 8.4 Scheduler 默认状态与用户预期不一致

正式工作区尚未配置自动能力时，实际默认值为：

```json
{
  "enabled": true,
  "reminders_enabled": true,
  "connector_jobs_enabled": true,
  "profile_maintenance_enabled": true,
  "channel_dispatch_enabled": true
}
```

初始化能力检查因此把 Scheduler 判断为已配置，Product 启动后立即生成一条全零成功记录。

这会造成三个问题：

1. “可选能力保持关闭”与实际默认值矛盾；
2. 用户没有主动开启却已经看到 Scheduler 运行；
3. 没有邮箱、数据源、Channel 时仍显示相应子任务已启用。

建议：

- 总 Scheduler 可默认启用，仅处理本地任务提醒；
- 数据源轮询、档案维护、Channel 分发默认关闭；
- 子能力只有依赖 ready 时才能开启；
- 初始化页提供“推荐自动化”预设，并明确会执行什么；
- 空跑不应作为普通用户可见的成功记录。

### 8.5 邮箱状态应拆成配置、验证和运行状态

当前邮箱已经区分 `configured`、`enabled` 和 `health_status`，方向正确。但 UI 主状态仍可能让用户困惑。

建议统一显示：

```text
账户：已保存/未保存
凭据：已保存/缺失
连接：已验证/未验证/失败
同步：自动开启/仅手动/关闭
最近同步：时间与结果
```

按钮状态也应基于表单当前值，而不是只基于上一次服务器配置。例如用户刚勾选“启用轮询”但尚未保存时，应明确提示“保存后生效”。

### 8.6 OpenCLI 与网站来源的依赖需要显式化

现在设置页将“OpenCLI 应用”和“牛客/BOSS”分到不同路由，逻辑上合理，但用户需要自己推断依赖。

目标页面应以来源卡片展示：

```text
牛客每日招聘信息
状态：被 OpenCLI 阻塞 / 待登录 / 可用 / 已启用 / 最近失败
依赖：OpenCLI、浏览器扩展、牛客登录态
动作：安装说明、检测、打开登录、立即同步
```

OpenCLI 可执行文件路径本身应放在高级详情中，普通用户只看到“已检测到/未检测到”。

### 8.7 QQ 配置过于开发者导向

`c2c:用户OpenID`、`group:群OpenID`、App ID、App Secret、事件订阅适合高级用户，不适合作为通用通知配置。

建议未来将通知渠道抽象为统一卡片：

```text
QQ
用途：任务提醒、申请进度、每日摘要、系统告警
状态：未连接
[连接 QQ]
```

连接流程内部再解释开发者平台和目标 ID。若暂时无法简化，至少应将 QQ 标记为“高级渠道”，避免让普通用户误以为首次初始化必须完成。

### 8.8 工作区创建后缺少安全的一键切换

设置页当前创建新工作区后只显示：

```text
重启 CareerConsole 后切换到新工作区。
```

没有“安全重启并切换”按钮，也没有明确说明当前数据仍写在哪个工作区。建议显示：

```text
当前运行工作区：A
待切换工作区：B
[取消切换] [检查并重启到 B]
```

在新实例 ready 前，旧实例继续服务。

### 8.9 导入导出需要更完整的本地文件体验

当前迁移包上传使用浏览器文件控件，恢复父目录使用手工输入。对 Windows 本地应用，建议：

- 导出后提供“打开所在文件夹”；
- 导入时使用系统文件选择器；
- 恢复目录使用系统目录选择器；
- 显示包版本、创建时间、文件数、是否含加密凭据；
- 导入前展示校验结果和覆盖/新建策略；
- 导入成功后提供安全切换和失败回滚。

## 9. 信息架构与导航审查

### 9.1 当前导航的主要问题

1. “求职总览”是核心页面，却位于“高级与诊断”。
2. “今日”和“全链路工作区”都提供概览，职责重叠。
3. 导航叫“招聘信息”，页面叫“招聘机会”。
4. “岗位库”不能直观表达它保存的是具体 JD。
5. “资料导入”与“职业档案”的先后关系不明确。
6. `/review` 事实审查没有主导航入口。
7. “待确认事项”进入统一队列后还需跳到不同业务页面处理。
8. 系统诊断项过多出现在主产品侧栏。

### 9.2 建议的目标导航

建议主导航围绕每日求职工作流重组：

```text
概览
- 今日
- 求职进展

机会
- 每日招聘
- 目标岗位

投递
- 申请进度
- 任务与日程
- 面试

我的材料
- 职业档案
- 简历与申请材料
- 资料来源

收件箱
- 招聘邮件
- 待我确认

设置
```

高级诊断从主侧栏移入“设置 → 高级”：

```text
数据来源诊断
Agent 运行记录
后台任务
系统状态
配置变更历史
```

### 9.3 页面职责建议

#### 今日

只回答“今天要做什么”：

- 今日和逾期任务；
- 即将截止岗位；
- 今日新增招聘；
- 新邮件和待确认；
- 即将面试；
- 自动任务异常。

#### 求职进展

只回答“整体进展怎么样”：

- 申请漏斗；
- 最近状态变化；
- 本周投递统计；
- 面试和 Offer；
- 跨模块一致性；
- 每日/每周摘要。

当前 `/workspace` 的业务总览内容应迁移到这里；备份、删除、垃圾回收等数据治理功能移入设置。

#### 每日招聘

对应牛客等信息源的聚合机会，只表示“招聘项目/批次/线索”，不直接等于可投递 JD。

#### 目标岗位

对应具体 JD 和冻结版本。建议将“岗位库”改为“目标岗位”或“具体岗位”，并在页面解释：

```text
每日招聘是线索；目标岗位是你确认并导入的具体 JD。
```

#### 申请进度

作为最核心的业务页面，承载：

- 当前阶段；
- 已投材料；
- 邮件证据；
- 待确认事件；
- 关联任务；
- 面试；
- 下一步建议；
- 完整不可变时间线。

#### 职业档案

将“档案概览、候选事实、已确认事实、求职偏好”放在同一个页面的清晰 Tab 中。业务记忆中的洞察、策略、每日摘要可作为“洞察与策略”二级区，不应和基本档案同等铺满。

#### 资料来源

只管理导入的原始简历、证明材料和来源历史。导入完成后提供“审查候选事实”主按钮。

#### 待我确认

统一队列应尽量支持就地处理；无法就地处理时，链接必须定位到具体实体，而不是只打开模块首页。

## 10. 明确缺陷与不一致

### 10.1 错误路由

申请详情中的邮件证据链接使用：

```tsx
<Link to="/mail">消息中心</Link>
```

实际路由是：

```text
/message-center
```

`/mail` 会命中通配路由并跳转到 `/status`。这是确定的坏链接。

### 10.2 开发阶段术语泄漏

用户界面仍出现：

- `Part 0`
- `Part 9`
- `CC-5`
- `Schema revision`
- `lease`
- `Connector`
- `AgentRun`
- `Drafter`
- `Reviewer`
- `revision`

其中部分术语可以在高级详情保留，但不应成为普通页面主文案。尤其 `Part 0/Part 9/CC-5` 是开发计划编号，发布界面应全部移除。

### 10.3 工作区治理文案与现有功能矛盾

全链路工作区写：

```text
恢复必须停止服务后使用 CLI。
```

设置页已经提供迁移包导入。需要统一为当前真实能力，并说明安全切换流程。

### 10.4 通配路由掩盖前端链接错误

当前所有未知路由都会跳到 `/status`。这会把拼写错误伪装成“用户进入了运行状态页”，难以发现。

建议：

- 提供真正的 404 页面；
- 在开发模式记录未知路由；
- 前端测试覆盖所有导航和业务链接；
- 不要把未知路径自动导向诊断页。

### 10.5 统一审查队列定位不精确

实际创建申请进度提案后，统一审查记录的 `target_url` 是：

```text
/application-review
```

它没有携带 proposal ID 或 application ID。待审项较多时，用户点击某一项后仍需再次寻找。

建议使用：

```text
/applications/<application_id>?review=<proposal_id>
```

或让统一审查中心直接完成确认/拒绝。

## 11. 状态、反馈与错误恢复

### 11.1 需要统一的能力状态模型

当前不同模块各自使用 `configured`、`enabled`、`healthy`、`installed`、`capability=true` 等概念。建议在前端 ViewModel 层统一：

| 状态 | 含义 |
|---|---|
| 未配置 | 没有必要配置 |
| 待验证 | 已保存但未完成连接/环境测试 |
| 可用 | 依赖和连接正常，但未开启自动运行 |
| 已启用 | 自动运行已开启 |
| 需重启 | 配置已保存，当前进程尚未应用 |
| 受阻 | 缺少上游依赖 |
| 异常 | 最近测试或运行失败 |

配置页、初始化完成页、Dashboard 健康提示应使用同一套状态。

### 11.2 保存反馈需要靠近操作对象

当前部分页面已有成功 notice，但还存在：

- 保存后只刷新数据，没有持续状态；
- 多个嵌入表单共用页面时，用户不确定保存了哪一块；
- 配置需重启时没有直接动作；
- 连接测试历史和当前连接状态分离；
- “保存配置”要求填写变更原因，干扰主要任务。

建议每张能力卡片都具备：

```text
当前状态
最近验证时间
保存按钮
保存中状态
保存成功摘要
测试按钮
测试结果
是否需要重启
```

### 11.3 错误恢复应给出可执行动作

错误消息目前主要展示后端 `message` 和 correlation ID。高级排障有用，但普通用户还需要：

- 哪一项失败；
- 当前数据是否已保存；
- 是否可以安全重试；
- 是否需要选择其他目录；
- 是否仍在旧工作区运行；
- 如何打开日志或复制诊断信息。

工作区切换、导入迁移包、Provider 测试、邮箱连接和 OpenCLI 检测应分别提供领域化错误操作。

## 12. 优先级问题清单

### P0：阻断发布或可能导致用户失去控制面

1. **工作区切换/重启没有双实例就绪检查和失败回滚。**
2. **正式用户部署形态未确定，源码进程同时承担服务、GUI 目录选择和自重启职责。**

### P1：核心流程错误或高概率造成配置失败

1. 目录验证不执行完整可写探针，验证成功不代表可创建和可启动。
2. 初始化“保存状态并继续”不真正保存嵌入表单。
3. 初始化步骤没有明确 configured/skipped/in-progress 状态。
4. Scheduler 和 Agent 默认启用状态与真实能力不一致。
5. 申请详情 `/mail` 链接错误。
6. 工作区创建后缺少安全的一键切换、取消和当前/待切换状态。
7. `/review` 事实审查没有主导航或导入后的直接入口。
8. 未知路由跳到系统状态页，掩盖链接错误。

### P2：影响理解效率和日常可用性

1. 核心“求职总览”藏在高级诊断。
2. “今日”和“全链路工作区”职责重叠。
3. “招聘信息/招聘机会/岗位库”术语不一致。
4. 岗位详情动作过多，没有推荐主路径。
5. 设置页普通参数和高级运维参数混排。
6. Provider 和 Agent Routing 对普通用户过于复杂。
7. 数据来源初始化步骤同时承载 OpenCLI、牛客、BOSS、邮箱。
8. 统一审查卡片不能精确定位具体待审对象。
9. 邮箱/OpenCLI/Channel 的配置、可用、启用、健康状态表达不统一。
10. 导入导出没有完整的系统文件/目录选择和安全切换体验。

### P3：文案、维护性和产品完成度

1. `Part 0/Part 9/CC-5` 等开发编号泄漏。
2. 英文 eyebrow 和工程术语密度过高。
3. README、Windows 安装、Workspace、迁移文档互相矛盾。
4. Scheduler 空跑记录对普通用户没有价值。
5. Provider 测试历史显示内部 ID。
6. 配置变更原因默认强制填写。
7. 全链路工作区混入备份、垃圾回收、危险删除等数据治理操作。

## 13. 建议的分阶段修改方案

### 第一阶段：安全启动与工作区

目标：任何配置或目录错误都不能让用户失去当前可用服务。

- 增加完整目录写入探针；
- 引入 pending workspace switch；
- 增加新实例 `/health/ready` 检查；
- 失败时回滚 active workspace；
- 设置页展示当前和待切换工作区；
- 增加安全重启、取消切换和恢复页；
- 明确 Windows 本地桌面形态与 Launcher 职责。

验收：

- 无权限目录在“验证目录”阶段被拒绝；
- 新工作区日志/数据库不可写时旧服务继续可用；
- 重启失败后 UI 能展示原因并取消切换；
- 进程退出后 Launcher 可重新启动服务。

### 第二阶段：初始化状态机

目标：用户能明确知道每一步是否完成、跳过或受阻。

- 持久化每步状态；
- 每步使用单一保存动作；
- 检测未保存修改；
- 显示能力影响矩阵；
- 职业档案步骤包含候选事实确认；
- 数据来源按“招聘来源/申请证据”拆分；
- 完成页显示 ready/enabled/blocked，而非布尔 capability。

验收：

- 任一步输入后点击“保存并继续”不会丢数据；
- 每个可选步骤都可明确跳过；
- 返回上一步时状态和输入一致；
- Provider 未配置时准确展示受影响功能；
- 完成初始化后未配置能力不会启动后台任务。

### 第三阶段：配置中心重构

目标：普通用户完成常见配置不需要理解内部架构。

- 常规、AI、数据来源、通知、自动化、数据安全、高级分层；
- Provider 预设和“保存并测试”；
- Agent 任务映射提供推荐预设；
- 统一能力状态组件；
- Scheduler 子能力按依赖启用；
- 将 revision、lease、审计细节移入高级区；
- 配置需重启时提供直接、安全的重启动作。

验收：

- 新用户能在不了解 Provider ID、lease、revision 的情况下完成配置；
- 无效 Agent 映射不能保存为“已启用”；
- 邮箱、OpenCLI、牛客和 QQ 的状态含义一致；
- 所有保存和测试都有明确反馈。

### 第四阶段：主导航与页面职责

目标：导航按用户任务组织，而不是按后端模块组织。

- 将全链路业务总览移出高级诊断；
- 区分“今日行动”和“求职进展”；
- 统一“每日招聘/目标岗位”术语；
- 职业档案增加事实审查入口；
- 数据治理移入设置；
- 提供真正的 404 页面；
- 删除开发阶段编号和非必要英文术语。

验收：

- 用户从首页最多两次点击可到达任一核心业务任务；
- 所有页面标题、导航名和空状态术语一致；
- 不再存在 `/mail` 等失效链接；
- 未知路由不会跳到系统状态页。

### 第五阶段：跨模块连续任务流

目标：底层已有的数据关联在前端形成连续操作。

- 导入简历后直接审查事实；
- 招聘线索明确转为具体 JD；
- 岗位详情提供推荐主路径；
- 材料 Final 后引导创建申请；
- 申请详情聚合任务、邮件、面试和待审进度；
- 统一审查中心支持精确定位或就地处理；
- Dashboard 的卡片直接进入对应上下文。

验收：

```text
首次建立档案
→ 找到招聘机会
→ 导入具体岗位
→ 生成材料
→ 建立申请
→ 接收邮件
→ 确认进度
→ 安排任务/面试
```

整条路径不需要用户理解内部 ID、Schema、Connector 或运行时概念。

### 第六阶段：Windows 交付与文档统一

目标：从“可运行源码”变成“可安装产品”。

- 选择桌面壳/Launcher 技术；
- 打包 Python Runtime 和前端产物；
- 提供安装、升级、卸载和数据保留策略；
- 提供开始菜单和桌面入口；
- 提供日志/诊断包导出；
- 统一 README 和 operations 文档；
- 明确 OpenCLI 是外部依赖及其安装/登录检查。

验收：

- 干净 Windows 环境无需手动安装 Python 即可启动；
- 首次打开自动进入初始化；
- 关闭和重启行为稳定；
- 升级不破坏工作区；
- 文档只保留一条普通用户安装路径。

## 14. 不建议在本轮重做的部分

为了控制范围，以下核心设计当前应保留：

- Domain → Application → Infrastructure/Interfaces 的业务分层；
- 工作区承载业务数据、机器级 Bootstrap 只保存激活路径；
- SQLite 迁移和 revision 检查；
- 职业事实确认后才进入正式业务；
- Agent 只生成候选，正式变化需要人工确认；
- 岗位和材料的不可变版本/事实快照；
- 申请事件流；
- 任务、提醒和北京时间边界；
- 邮箱只读和最长 30 天扫描；
- 牛客自动只同步当天、历史最长手动回看 30 天；
- OpenCLI 作为外部应用使用，而不是在 CareerConsole 中开发其源码；
- Channel 作为通知出口；
- Scheduler 作为统一定时基础设施。

需要调整的是这些能力在产品中的启用方式、状态表达、入口和衔接，不是删除这些能力。

## 15. 推荐的下一步

建议后续直接从第一阶段开始实施，先解决：

1. 工作区完整可写验证；
2. 安全切换与重启失败回滚；
3. 当前/待切换工作区状态；
4. 初始化步骤状态机；
5. Scheduler/Agent 默认能力状态。

完成后再进行导航和视觉改造。原因是前端布局即使先变得清晰，底层初始化和切换仍可能让用户在第一天就失去服务；启动与配置可靠性应先成为稳定边界。

